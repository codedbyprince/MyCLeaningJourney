#include <stdio.h>
int main() {
     int length, breadth;
      printf("Enter length\n");
      scanf("%d", &length);
      
      printf("Enter breadth\n");
      scanf("%d", &breadth);Added Chapter 1 - Q1 programAdded Chapter 1 - Q1 program
      printf("The area of this rectangle is %d", length*breadth);
  return 0; 
}